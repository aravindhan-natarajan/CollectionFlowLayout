# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'PhotosList' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for PhotosList

   pod 'SDWebImage', '~> 5.0'

  target 'PhotosListTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'PhotosListUITests' do
    # Pods for testing
  end

end
